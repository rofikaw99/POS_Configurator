const api_general = require(`${process.cwd()}/tests_explorer/object_repository/api/general`)    // Sesuaikan dengan nama file js nya. Kalau ada lebih dari satu, bisa ditambah di bawah nya

module.exports = {
    apigeneral: api_general                                                                     // Disesuaikan dengan deklarasi di atas
}